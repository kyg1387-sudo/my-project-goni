# AI 드라마 영화 제작 파이프라인

이 저장소는 AI 생성 영상으로 한국 드라마(가로 16:9 롱폼, 5~15분)를 만드는 파이프라인이다.
새 작품을 만들 때는 아래 규칙을 기본으로 적용한다.

**제작자의 목표 (사용자 확정)**: "모든 사람들이 내가 만든 영상을 한 번쯤은 볼 수
있게 하는 것." 이에 따라 완성작마다 다국어 자막(CC .srt — 일본어부터, 반응에 따라
영어 등 확장)과 유튜브 언어별 제목·설명을 기본 산출물로 준비한다(무료 경로 우선).
방식: `subs/<작품>.ja.srt` + `docs/일본판-<작품>-메타데이터.md` 참고(EP1 선례).
화자 표기 `（キム）`식 접두, 화면 자막 카드도 번역 포함, 최종 트리밍 후 타이밍 재맞춤.
**글로벌 가이드 약속 (사용자 요청 — 매 작품 필수)**: 작품이 완성돼 사용자에게
전달할 때마다, 업로드 단계에서 따라 할 수 있는 "글로벌 진출 가이드"를 반드시 함께
제공한다 — ① 다국어 자막 파일 전달 + 유튜브 CC 업로드 순서(처음 하는 사람 기준으로
단계별 상세히), ② 언어별 제목·설명 복사용 원문, ③ 유튜브 분석에서 국가별 반응
확인하는 법과 다음 언어 확장 판단 기준, ④ 반응 좋을 때의 다음 단계(더빙판 등) 제안.
사용자가 요청하지 않아도 먼저 챙겨서 안내할 것.
또한 사용자는 유튜브 운영 경험을 쌓아가는 중이므로(사용자 요청: "많은 정보도
알려줘야 해"), 제작·업로드·분석 각 단계에서 알아두면 좋은 정보(알고리즘 동작,
분석 지표 읽는 법, 채널 성장 팁, 비용 절감법)를 그때그때 먼저 알려줄 것.

**제작 문법 규칙집 (사용자 지정 — 다음 작품부터 시스템 규칙으로 적용)**:
`docs/영화제작규칙집.md` — Seedance 2.0 기준 제작 순서(캐릭터 시트 3패널 →
로케이션 시트 → 스토리보드 콘택트시트 → 5블록 컷 프롬프트), 컷 길이 기준표,
카메라 문법(Movement/Angle/Size 3축), 검열 규칙, 내보내기 전 체크리스트.
장면 프롬프트를 생성할 때 이 규칙집의 5블록 양식과 카메라 문법을 따를 것.
아래 파이프라인 고유 규칙(한국어 전용, 화자-화면 일치, 글자 금지, 비용 원칙 등)과
충돌하면 아래 규칙이 우선한다.

## 제작 원칙 (품질 기준)

1. **화자-화면 일치**: 대사가 나오는 동안 화면에는 반드시 그 대사를 말하는 인물이
   나와야 한다. 한 장면(클립)에는 한 화자의 대사만 배치한다.
2. **드라마 형식 + 내레이션 (사용자 확정, EP1에서 변경)**: 무언 몽타주 구간에는
   따뜻한 3인칭 스토리텔러 내레이션을 넣는다 — 미담 사연 장르의 표준이며 중장년
   시청자 몰입에 효과적. 단, 대사 장면과 겹치지 않게 무언 구간에만 배치하고,
   내레이터 목소리는 배역들과 구별되는 별도 목소리로 오디션 후 확정한다.
   내레이션 줄은 .ass의 Naration 스타일 + 오디오 설정 narration_styles 등록
   (=립싱크 제외). 시간·장소 경과와 인용문은 여전히 자막 카드(Caption)로 표시.
3. **한국어 전용**: TTS는 language_boost=Korean, 장면 프롬프트에 "한국어로 말하는
   입 모양, 영어 없음"을 명시한다. TTS·현장음 생성 후에는 `오디오 언어 검사`
   (qa-language.yml, 무료)로 전 파일의 언어를 검사해 외국어 섞임 0건을 확인한 뒤
   완성본을 만든다. 현장음(mmaudio)은 negative_prompt를 넣어도 사람 말소리를
   자주 섞는다(2회 실증) — 인물이 나오는 장면의 룸톤·웅성거림형 현장음은 쓰지
   말고(빈 프롬프트=생략), 효과음형(그릇·문·엔진·새소리)만 쓰되 QA로 검증해
   말소리가 나온 그룹은 제거한다. 한 음절짜리 감탄사 대사("뭐?")는 외국어처럼
   들릴 수 있으니 두 음절 이상("뭐라고?")으로 쓴다.
4. **립싱크**: 대사 장면 프롬프트에는 "정면 상반신, 입을 자연스럽게 움직이며"를
   명시하고, 고개 숙임·인사 같은 동작 장면에는 대사를 넣지 않는다(무언 Action 장면).
5. **인물 일관성**: 등장인물 외형은 고정 문구(상수)로 정의해 모든 장면 프롬프트에
   동일하게 반복한다. 짧은 문구로는 부족하다(실증): 얼굴형·머리 모양·의상을
   구체적으로 고정하고("둥근 얼굴, 짧고 단정한 검은 올림머리, 베이지색 트위드
   재킷 정장과 흰 블라우스"식), 클로즈업에도 반드시 장소 문구를 넣는다(장소가
   빠지면 배경과 인물이 전혀 다르게 생성됨). 완성 전 전 장면 프레임 검수에서
   같은 인물이 다른 사람처럼 보이는 장면은 찾아 재생성한다. 다음 작품부터는
   인물별 기준 초상 이미지를 먼저 1장 만들고 image-to-video로 장면을 생성하는
   방식을 검토할 것 — 텍스트 프롬프트만으로는 동일 배우 수준의 일치가 어렵다.
6. **자연스러운 대화 리듬**: 주고받는 대사 사이 간격은 1~2초를 목표로 한다.
   장면(5/10초 블록)이 대사보다 길어 남는 꼬리 공백은, 실제 TTS 길이를 잰 뒤
   scene_durations를 줄여 잘라낸다(대사 시작 오프셋은 장면 안에서 유지 —
   그래야 립싱크·TTS 캐시를 그대로 재사용한다). 극적 멈춤이 필요한 곳만 예외.
7. **감정 연기 일치**: 화내는 대사는 angry, 웃는 대사는 happy처럼 TTS 감정
   (emotion_overrides)을 대사 내용과 반드시 맞추고, 장면 프롬프트의 표정 묘사
   (화난 표정/비웃는 표정 등)도 같은 감정으로 명시해 목소리-표정을 일치시킨다.
8-1. **장면 전환 연출 (사용자 확정 EP2~ — 대사 끝 어색함 제거)**:
   ① 대사가 끝나면 바로 컷하지 않고 0.5~1초의 리액션/여운(듣는 이의 표정,
   말한 이의 잔여 감정)을 두고 전환한다 — 단, 침묵 꼬리 몸짓 문제(교훈②)가
   없는 길이로 재단. ② 무언 몽타주·시간 경과 전환은 하드 컷 대신
   디졸브(ffmpeg xfade 0.4~0.6초)를 쓴다(조립 단계에서 지정 전환에 적용).
   ③ 현장음·BGM을 컷 너머로 이어지게 배치해 소리로 장면을 잇는다(브리지).
   ④ 감정 온도가 급변하는 컷 사이에는 무언 완충 컷(풍경·소품)을 1개 둔다.
8-2. **줄별 톤 연출표 (사용자 확정 EP2~)**: 대본 확정 시 모든 대사에
   [감정(emotion), 속도(speed), 앞뒤 침묵 간격, 연기 지시(EL 오디오 태그
   [trembling]/[whisper]식 또는 장면 프롬프트 표정 지시)]를 줄 단위로 표로
   작성해 오디오 설정에 빠짐없이 반영한다 — 빈 줄(지정 없는 대사) 0개.
8. **콜드 오픈 (사용자 확정 — 모든 작품 기본 적용)**: 타이틀 전에 강렬한
   훅 장면(20~30초)을 먼저 배치한다 — 유튜브 초반 30초 이탈률이 노출을
   좌우하기 때문. 방식: 후반부의 미스터리·클라이맥스 장면을 앞으로 당겨
   재배치(새 장면 추가 없이 = 비용 0원)하고, 궁금증 자막 카드 1장 +
   타이틀 카드로 잇는다. 본편 시작은 「사흘 전」식 자막 카드로 역행.
   콜드 오픈 구간은 그대로 쇼츠 훅으로 재사용한다. (실증: EP1 경비원
   김씨의 비밀 — CCTV 미행 훅 → 「그 남자는, 경비원 김씨였다」 → 타이틀)

## 파일 구조

- `scripts/build_full_assets.py` — 대본(SECTIONS) → 장면/자막/오디오 설정 3종 생성기.
  새 작품은 이 파일의 SECTIONS 구조(beat 정렬 방식)를 복사해 만든다.
  실행하면 화자-장면 정렬이 자동 계산된다. 생성 후 반드시 정렬 검증을 돌릴 것.
- `scripts/scenes/<작품>.json` — 장면 프롬프트. 항목은 `{"prompt", "duration": 5|10}`.
- `subs/<작품>.ass` — 자막. 스타일: 화자별 색상 + Naration(보이스오버) + Caption(무음 카드).
- `scripts/audio/<작품>.json` — 목소리·감정·현장음·BGM 설정.
  `narration_styles`=립싱크 제외, `silent_styles`=TTS 제외(화면 전용).
- `scripts/generate_video.py` — 장면별 클립 생성 (Ark Seedance / fal.ai 폴백,
  장면별 5/10초, out/sceneNN.mp4가 이미 있으면 재사용 = 이어하기).
- `scripts/generate_audio.py` — TTS·립싱크·현장음·BGM 생성 후 믹싱.

## 제작 순서 (GitHub Actions)

1. `콩트 영상 생성` (generate-video.yml): inputs `skit=<작품>`.
   실패 시 같은 워크플로를 `resume_run_id=<실패한 run id>`로 재실행하면
   이미 생성된 장면을 재사용한다.
2. `콩트 영상 자막 입히기` (burn-subtitles.yml): inputs
   `skit=<작품>, source_run_id=<1번 run id>, add_audio=true, lipsync=true, commit_output=true`.
   중간 실패 시 `resume_run_id`로 TTS/립싱크 산출물을 복원해 이어한다.
3. 완성 영상은 브랜치 `deliveries/`에 커밋된다. 100MB 초과 시 90MB 조각으로
   분할되며 `cat <이름>.part-* > <이름>.mp4`로 복원한다.

API 키는 GitHub Secrets(FAL_API_KEY, ARK_API_KEY)에만 둔다. 코드/채팅에 넣지 말 것.

## 비용 원칙 (소액 제작 — 반드시 지킬 것)

제작자는 소액 예산으로 최고 품질을 목표로 한다. 생성 API(fal.ai)는 결과가
나빠도 과금되므로:

1. 생성 실행 전에 무료로 잡을 수 있는 오류(대본·화자 배치·타이밍·프롬프트)는
   전부 검증하고 시작한다. 검증 없이 생성 워크플로를 돌리지 않는다.
2. 재생성은 항상 부분(regen_scenes / invalidate)으로 — 전체 재생성은 구조가
   바뀌었을 때만, 그 경우에도 캐시(TTS·BGM·정상 장면)를 최대한 재사용한다.
3. 비용이 드는 실행 전에는 규모(다시 만들 장면 수)를 사용자에게 먼저 알린다.
4. 과거 오생성 패턴을 프롬프트 규칙으로 반영해 재발을 막는다:
   무인(인물 없음) 장면에도 모델이 임의 인물을 만들어 넣는다(실증: 빈 등굣길에
   가짜 소년 — 인물 일원화 위반). 무인 장면은 "사람이 한 명도 없는, 인물 없음"을
   이중으로 명시하고, 프레임 검수 때 '무인 지시 장면에 인물 등장'을 명시적
   불합격 항목으로 검사한다. 임의 등장 인물은 배경 인물로 보이더라도 기준 배우와
   다른 사람이므로 반드시 재생성(기준 초상 참조로 교체하거나 무인으로 재시도).
   클로즈업에는 소품 없는 인물 문구(DOYUN_FACE식), 단독 장면에 "혼자/단독 인물 샷",
   중의어 회피(예: 산소→무덤 봉분), 액션 장면에도 장소 문구 포함.
   화면 속 새겨진 글자(비석·간판·문서)는 생성 모델이 못 쓴다 — 한자 한 글자
   지시도 가짜 문자로 번짐(실증). 항상 "아무 글자도 없는 무지/매끈한 표면"으로
   지시하고, 옷·소품도 무지(로고·인쇄 없음)로 고정할 것.
5. 완성본은 사용자에게 보내기 전에 전 장면 프레임 검수를 통과해야 한다.
6. **실수 비용 원칙 (사용자 확정)**: 사용자의 추가 요청에 드는 비용은 정당하지만,
   AI(제작 보조)의 실수·검수 누락으로 생기는 재생성 비용은 부당하다.
   실수로 인한 수정은 반드시 무과금 경로(기존 클립 편집·재사용, video-overrides,
   audio-overrides, 캐시 재조합)를 먼저 찾는다. 무과금 경로가 불가능해 유료
   재생성이 불가피하면, 실수로 인한 비용임을 명시하고 사용자 승인을 받은 뒤에만
   실행한다.

## 검증 (생성 자산 커밋 전 필수)

- 자막의 모든 대사 줄이, 그 시간대 장면 프롬프트의 화자와 일치하는지 초 단위로 검사
  (이 세션들에서 쓰는 검증 스크립트: 장면 경계·화자·MUTE 여부 확인, 불일치 0건이어야 함).
- emotion_overrides 인덱스는 silent_styles 제외 후의 TTS 줄 번호 기준이다.
- 자막(.ass)에 화면 전용 스타일을 새로 추가하면(NameTag 등) 반드시 오디오 설정의
  silent_styles에도 등록할 것 — 빠뜨리면 TTS 줄 번호가 밀려 화면 인물과 목소리가
  어긋난다(실증: 명찰 오버레이 사고).

**성장 전략 체크리스트 (사용자 확정 — "잊어버리니까 제작할 때마다 꼭 말해줘"):**
매 작품의 기획·완성·업로드 단계에서 아래를 사용자에게 먼저 상기시킬 것.
① 시리즈 세계관 — 같은 아파트 단지 배경, 기존 인물(경비원 김씨·지우·박여사 등)
  카메오 재등장 + 영상 끝 다음 화 예고(구독 전환의 핵심, 초상·장소 재사용=비용 절감).
② 데이터 기반 기획 — 직전 쇼츠 성적(소재·첫 문장 스타일)을 보고 다음 본편 소재 결정.
③ 본편 1개 → 쇼츠 5개(콜드오픈 훅/명대사/눈물 장면/반전 직전 끊기/AI 제작 비하인드),
  매일 1개 업로드 리듬 유지.
④ 업로드 시간 — 본편은 금·토 저녁 7~9시 예약(중장년 피크), 쇼츠는 매일.
⑤ 시청자 참여 — 아웃트로·고정 댓글에 질문 넣기("여러분 단지에도 이런 분 계신가요?").
⑥ 썸네일 A/B — 인물 클로즈업+큰 글씨 3~4단어로 2~3종 만들어 Test & Compare 걸기.
⑦ 공통 아웃트로 — 진행자(assets/portraits/outro-host, 사용자 선택 초상)가 구독·좋아요
  요청 + 다음 화 예고를 말하는 8초 장면을 모든 작품 끝에 재사용.

**EP1 실증 교훈 (다음 작품 제작 전 필수 점검 — 실수 재발 방지):**
① 입모양 정밀 생성(omnihuman)은 장면의 **첫 프레임 1장**으로 만든다 — i2v 장면의
  첫 프레임 = 기준 초상 그대로이므로, **기준 초상이 그 장면의 계절·의상·장소와
  일치해야 한다**(실증: 봄 반팔 초상 → 겨울 장면이 반팔로 생성됨). 계절·의상이
  다른 장면은 전용 초상(예: kim-winter)을 먼저 만들어 ref로 쓸 것.
② omnihuman은 **침묵 구간에도 말하는 몸짓**을 만든다(실증) — 대사 장면의
  scene_duration을 대사 길이에 최대한 맞추고(규칙 6), 긴 침묵 꼬리가 불가피하면
  대사 종료 지점에서 여운 정지컷(줌 홀드)으로 교체.
③ **대본 인과 순서 검증**: "아니," "그래서" 등 반응으로 시작하는 대사는 반드시
  원인 행동/대사 뒤에 오도록 대본 단계에서 검사(실증: 택배 장면 순서 역전).
④ **소품·의상 연속성**: 장면 간 이어지는 소품(보온병·코트·우산)은 "받는 장면 →
  들고 있는 장면 → 벗어주는 장면"처럼 앞뒤 장면 프롬프트에 함께 명시(실증:
  유자차 받는 동작이 엉뚱한 장면에, 코트 없이 코트를 벗어줌).
⑤ 정지컷·썸네일용 프레임은 **자막이 화면에 없는 순간**에서 추출할 것(실증 2회).
⑥ 프레임 검수는 장면당 시작·중간·끝 3지점 + **바닥·배경 상태**(눈·계절감)까지
  불합격 항목으로 검사(실증: 빙판길 지시가 마른 보도로 생성).
⑦ 무과금 수리 도구: burn-subtitles의 lipsync=remix(완성본 영상 트랙 재사용
  오디오 재조합), remix_patch(지정 장면만 omnihuman 재생성·구간 교체),
  로컬 ffmpeg 블록 스왑(화면+소리 통째 교환)·구간 교체·여운 컷. 아티팩트에
  omni*.mp4 보존됨(재실행 시 재과금 없음).
⑧ resume_run_id는 **성공한 run의 번호인지 아티팩트 존재부터 확인** 후 지정
  (실증: 취소된 run 번호를 넣어 실패). fal 충전 직후엔 반영 지연 잠금이 있을 수
  있으니 실패 시 몇 분 뒤 재시도(실패 요청 과금 0원).

**품질 도구 제안 약속 (사용자 요청)**: 영상 퀄리티를 높일 수 있는 새 생성
모델·API·도구가 있으면, 필요 시점에 비용·효과·필요한 키와 함께 먼저 제안할 것.

## 다음 작품 시작 시 반드시 안내할 것 (사용자 요청)

- 제작 시작 시 사용자(김영곤)에게 먼저 알릴 것: ① 인물별 기준 초상 이미지를
  만들어 image-to-video로 생성하면 인물 일관성이 고정되고 재생성 비용이 크게
  줄어든다는 것, ② 본인 정면 사진을 첨부하면 등장인물 중 한 명(예: 경비원)을
  본인 얼굴로 카메오 출연시킬 수 있다는 것(얼굴 교체 방식, 소액).
- ③ 감정표현력 강화(사용자 확정 — 이번 작품이 감정표현이 낮았다는 피드백):
  MiniMax 기반으로 (1) 전 대사에 emotion_overrides 지정(빈 줄 없이),
  (2) 대본에 감탄사·말줄임·느낌표 등 감정이 실리는 문장 작법 적용,
  (3) 본편 제작 전 후보 목소리 시험 생성 후 사용자 청취로 배역 선정,
  (4) 줄별 속도·침묵 간격을 감정에 맞춰 연출,
  (5) 장면 프롬프트에 구체적 감정 연기 지시("눈물이 고인 채 떨리는 목소리로"식).
  미묘한 연기가 필요한 핵심 장면 2~3개는 ElevenLabs 등 상위 TTS로 만들어
  비교 청취 제안(일부만이라 소액).
- ④ 카메오 명찰(사용자 확정): 카메오 배역(경비원 등)의 명찰 이름은 "김영곤".
  방식(사용자 제안): 기준 초상 생성 시 글자 없는 실물 명찰표(배지)를 착용시키고,
  정지 초상 사진 위에 실제 한글 폰트로 "김영곤"을 합성한 뒤 그 사진으로
  image-to-video 생성 — 명찰표가 옷의 일부로 따라다니게 한다.
  본편 전 시험 장면 1개로 글자 유지 여부를 검증하고, 근접 장면에서 글자가
  뭉개지면 명찰 클로즈업 정지 인서트 컷(폰트 합성)을 보조로 쓴다.
  (움직이는 인물 위 자막 오버레이 방식은 어색하다는 피드백으로 폐기 — 실증)
  사용자가 잊고 있어도 제작 시작 단계에서 먼저 물어볼 것.
- ⑤ 대화 자연스러움 개선 (2작품 완성 시 사용자 피드백 — "약간 대화의 어색함이
  있다"): 다음 작품 대본 단계에서 (1) 주고받는 대사를 문어체가 아닌 구어체로
  다듬고(같은 정보라도 짧게 끊어 말하기, 반말/존대 일관성), (2) 대사 사이
  리액션 컷(듣는 인물의 표정)을 스토리보드에 넣어 핑퐁 리듬을 만들고,
  (3) 립싱크 정밀도 한계(한국어 음소 입 모양 근사치)는 대사 클로즈업 장면을
  오디오 구동 생성(OmniHuman류, 장면당 $1~2)으로 만들어 보완 — 본편 전 시험
  장면 1개로 품질 확인. 텍스트 프롬프트 장면과 섞어 쓰되 대사 없는 장면은
  기존 방식 유지.
